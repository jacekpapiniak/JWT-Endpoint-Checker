meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: http://localhost:5250/api/login
  body: json
  auth: inherit
}

body:json {
  {
    "Email" : "{{email}}",
    "Password" : "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
